/** Maximum width for mobile breakpoint (matches Tailwind's sm breakpoint) */
export const MOBILE_MAX_WIDTH = 639;

/** Swipe distance threshold in pixels to trigger card cycle */
export const SWIPE_THRESHOLD = 20;

/** Vertical offset for stacked cards in mobile view */
export const STACK_TOP_OFFSET = 5;

/** Horizontal offset for stacked cards in mobile view */
export const STACK_LEFT_OFFSET = 15;
